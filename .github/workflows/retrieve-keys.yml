name: Retrieve API Key from Vault

on:
  push:
    branches:
      - master

jobs:
  get-api-key:
    runs-on: ubuntu-latest


    steps:
        - name: Import Secrets
          id: import-secrets
          uses: hashicorp/vault-action@v2
          with:
            url: https://54.209.241.113:8200
            caCertificate: ${{ secrets.CA_CER_PATH}}
            token: ${{ secrets.VAULT_TOKEN }}
            secrets: |
                secret/data/capstone api-key | API_KEY;
